# Application
NODE_ENV=production
PORT=4000
API_PREFIX=api/v1
CORS_ORIGINS=http://localhost,http://65.2.75.137

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=ccs_user
DB_PASSWORD=StrongPassword123!
DB_NAME=celebration_cake_shop
DB_SSL=false
DB_POOL_SIZE=10

# JWT
JWT_SECRET=change-this-to-a-very-long-random-secret-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=change-this-refresh-secret-too
JWT_REFRESH_EXPIRES_IN=30d

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
AUTH_THROTTLE_LIMIT=10

# App
MAX_FILE_SIZE=10mb
BCRYPT_ROUNDS=12
